const Nav = () => {
  const linkGiver = () => {
    onclick = (e) => {};
  };

  return (
    <div>
      <button>HOME</button>
      <button>GALLERY</button>
      <button>CONTACT</button>
    </div>
  );
};

export default Nav;
